# buchikun

A Rust library.

## Installation

Add this to your `Cargo.toml`:

```toml
[dependencies]
buchikun = "0.1.0"
```

## Usage

```rust
use buchikun::add;

let result = add(2, 2);
assert_eq!(result, 4);
```
